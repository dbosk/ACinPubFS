\usepackage[utf8]{inputenc}
\usepackage[T1]{fontenc}
\usepackage{fixltx2e}
\usepackage[british]{babel}
\usepackage[defblank]{paralist}
\usepackage[fulladjust]{marginnote}
\usepackage[underline=false]{pgf-umlsd}
\usepackage{tikz}
\usepackage{algorithm}
\usepackage[noend]{algpseudocode}
\usepackage{graphicx}
\usepackage{booktabs}
\usepackage{subfig}
\usepackage{mleftright}

\usepackage{xparse}
\NewDocumentCommand{\head}{O{45} O{1em} m}{%
  \makebox[#2][l]{\rotatebox{#1}{#3}}
}

\usepackage{amssymb}
\usepackage{amsmath}
% Common algorithms
\DeclareMathOperator{\Aop}{\mathcal{A}}
\NewDocumentCommand{\A}{o}{\ensuremath{%
    \Aop%
    \IfValueT{#1}{%
      \mleft( #1 \mright)%
    }
  }}
\DeclareMathOperator{\Dop}{\mathcal{D}}
\NewDocumentCommand{\D}{o}{\ensuremath{%
    \Dop%
    \IfValue{#1}{%
      \mleft( #1 \mright)%
    }
  }}
%\DeclareMathOperator{\Fop}{\mathcal{F}}
%\NewDocumentCommand{\F}{o}{\ensuremath{%
%    \Fop%
%    \IfValueT{#1}{%
%      \mleft( #1 \mright)%
%    }
%  }}
% File system
\DeclareMathOperator{\FSop}{\mathcal{FS}}
\NewDocumentCommand{\FS}{o}{\ensuremath{%
    \FSop%
    \IfValueT{#1}{%
      \!.\mathsf{#1}%
    }
  }}
\NewDocumentCommand{\fscreate}{o o}{\ensuremath{%
    \FS[create]%
    \IfValueT{#1}{%
      \mleft( #1, \IfValueTF{#2}{#2}{\cdot} \mright)%
    }
  }}
\NewDocumentCommand{\fsread}{o}{\ensuremath{%
    \FS[read]%
    \IfValueT{#1}{%
      \mleft( #1 \mright)%
    }
  }}
\NewDocumentCommand{\fsappend}{o o}{\ensuremath{%
    \FS[append]%
    \IfValueT{#1}{%
      \mleft( #1, \IfValueTF{#2}{#2}{\cdot} \mright)%
    }
  }}

% Communication model
\DeclareMathOperator{\Cop}{\mathcal{C}}
\NewDocumentCommand{\C}{m o}{\ensuremath{%
    \Cop\mleft[ #1 \mright]%
    \IfValueT{#2}{%
      .\mathsf{#2}%
    }
  }}
\NewDocumentCommand{\csetup}{m o}{\ensuremath{%
    \C{#1}[setup]%
    \IfValueT{#2}{%
      \mleft( #2 \mright)%
    }
  }}
\NewDocumentCommand{\cpub}{m o o}{\ensuremath{%
    \C{#1}[publish]%
    \IfValueT{#2}{%
      \mleft( #2, \IfValueTF{#3}{#3}{\cdot} \mright)%
    }
  }}
\NewDocumentCommand{\cget}{m o}{\ensuremath{%
    \C{#1}[get]%
    \IfValueT{#2}{%
      \mleft( #2 \mright)%
    }
  }}

% Push Model
\DeclareMathOperator{\PushOp}{\mathcal{P}_n}
\NewDocumentCommand{\Push}{m o}{\ensuremath{%
    \PushOp\mleft[ #1 \mright]%
    \IfValueT{#2}{%
      .\mathsf{#2}%
    }
  }}
\NewDocumentCommand{\pnsetup}{m o}{\ensuremath{%
    \Push{#1}[setup]%
    \IfValueT{#2}{%
      \mleft( #2 \mright)%
    }
  }}
\NewDocumentCommand{\pnpub}{m o o}{\ensuremath{%
    \Push{#1}[publish]%
    \IfValueT{#2}{%
      \mleft( #2, \IfValueTF{#3}{#3}{\cdot} \mright)%
    }
  }}
\NewDocumentCommand{\pnget}{m o}{\ensuremath{%
    \Push{#1}[fetch]%
    \IfValueT{#2}{\mleft( #2 \mright)}
  }}
% Pull Model
\DeclareMathOperator{\PullOp}{\mathcal{P}_1}
\NewDocumentCommand{\Pull}{m o}{\ensuremath{%
    \PullOp\mleft[ #1 \mright]%
    \IfValueT{#2}{%
      .\mathsf{#2}%
    }
  }}
\NewDocumentCommand{\psetup}{m o}{\ensuremath{%
    \Pull{#1}[setup]%
    \IfValueT{#2}{%
      \mleft( #2 \mright)%
    }
  }}
\NewDocumentCommand{\ppub}{m o o}{\ensuremath{%
    \Pull{#1}[publish]%
    \IfValueT{#2}{%
      \mleft( #2, \IfValueTF{#3}{#3}{\cdot} \mright)%
    }
  }}
\NewDocumentCommand{\pget}{m o}{\ensuremath{%
    \Pull{#1}[fetch]%
    \IfValueT{#2}{\mleft( #2 \mright)}
  }}


\usepackage[strict]{csquotes}
%\MakeBlockQuote{|}{§}{|}
%\EnableQuotes{}

\usepackage[lncs,crypto,stdmaths,algebra,ac,surveillance,std]{libbib}

\usepackage[natbib,style=lncs,backend=biber]{biblatex}
\addbibresource{anon.bib}
\addbibresource{crypto.bib}
\addbibresource{meta.bib}
\addbibresource{osn.bib}
\addbibresource{surveillance.bib}
\addbibresource{ac.bib}
\addbibresource{be.bib}
\addbibresource{rfc.bib}

\usepackage[binary,amssymb]{SIunits}
\usepackage{cleveref}
